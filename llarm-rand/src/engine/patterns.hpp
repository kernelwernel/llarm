#include "../params.hpp"

#include <llarm/shared/types.hpp>

namespace patterns {
    u32 logic_arithmetic(const param_struct params);
    u32 memory(const param_struct params);
    u32 load_store(const param_struct params);
}